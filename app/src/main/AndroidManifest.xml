<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.muhammadtalhaminhas.project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SubjectSelection"></activity>
        <activity android:name=".AcadmyForm" />
        <activity android:name=".StudentForm" />
        <activity android:name=".TeacherForm" />
        <activity android:name=".AccountType" />
        <activity android:name=".Register" />
        <activity android:name=".Payment_Succesful" />
        <activity android:name=".Edit_profile" />
        <activity android:name=".Settings" />
        <activity android:name=".SignIn" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>